<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.walkinclinic">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".EditClinicHoursEmployee"></activity>
        <activity android:name=".ServicesEmployee" />
        <activity android:name=".ClinicEmployee" />
        <activity android:name=".EditProfileEmployee" />
        <activity android:name=".ListOfHoursEmp" />
        <activity android:name=".ProfileEmployee" />
        <activity android:name=".userAdmin" />
        <activity android:name=".ClinicList" />
        <activity android:name=".walkinclinicAdmin" />
        <activity android:name=".servicesAdmin" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegisterActivity" />
        <activity android:name=".WelcomeActivity" />
        <activity android:name=".LoginActivity" />
    </application>

</manifest>